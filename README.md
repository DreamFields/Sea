# 使用方法
## 先用根据umi官网的指导安装好umi以及yarn包管理工具到电脑上（npm没有yarn好用）。然后选择一个空目录，把我这个git仓库中的文件复制到该目录中或者直接将git中文件下载到本地，然后在终端cd到这个本地仓库的根目录下，输入指令 yarn 安装好所有依赖（这个过程会很久很久，这个是正常现象），在vscode中打开，可以使用vscode的终端也可以直接在终端中输入命令 umi dev，稍等一会儿项目就会部署到localhost：8000端口。
